// Search and Replace
/* function myReplace(str, before, after) {
  let find = str.match(before);
  if (/[A-Z]/.test(find)) {
    let upper = after.charAt(0).toUpperCase() + after.slice(1);
    return str.replace(before, upper);
  } else {
    let lower = after.charAt(0).toLowerCase() + after.slice(1);
    return str.replace(before, lower);
  }
}
console.log(myReplace("I think we should look up there", "up", "Down")); */
